name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  shell-and-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Validate shell syntax
        run: |
          bash -n setup.sh
          bash -n .totonoe/bin/*.sh

      - name: Validate SQL migrations (sequential apply)
        run: |
          db="$(mktemp)"
          for f in $(ls .totonoe/migrations/*.sql | sort); do
            sqlite3 "${db}" ".read ${f}" || { echo "FAIL: ${f}"; exit 1; }
          done
          ver="$(sqlite3 "${db}" 'SELECT MAX(version) FROM schema_version;')"
          echo "schema_version after all migrations: ${ver}"
          rm -f "${db}"

      - name: Validate JSON files
        run: |
          jq . .claude/settings.json >/dev/null
          jq . .totonoe/schemas/judge.schema.json >/dev/null
          jq . .totonoe/schemas/reviewer.schema.json >/dev/null
          jq . .totonoe/config.json >/dev/null

      - name: Smoke test setup and local runtime flow
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          test -f "${target}/.claude/agents/GENERIC-ENGINEER.md"
          test -f "${target}/.claude/agents/SECURITY-ENGINEER.md"
          test -f "${target}/.claude/agents/TEST-ENGINEER.md"
          test -f "${target}/.claude/agents/PERF-ENGINEER.md"
          test -f "${target}/.claude/agents/REFACTOR-ENGINEER.md"
          test -f "${target}/.env.example"
          (
            cd "${target}"
            .totonoe/bin/init_job.sh --job-name smoke --goal-template feature_loop
            .totonoe/bin/pause_job.sh --job-name smoke --reason "ci pause test"
            .totonoe/bin/status.sh --job-name smoke --json >/dev/null
            .totonoe/bin/resume_job.sh --job-name smoke
            printf 'summary\n' > .totonoe/runtime/smoke/summary.md
            .totonoe/bin/record_claude_round.sh \
              --job-name smoke \
              --summary-file .totonoe/runtime/smoke/summary.md \
              --changed-file CLAUDE.totonoe.template.md \
              --quality-analyze passed \
              --quality-test skipped
            .totonoe/bin/status.sh --job-name smoke --json --provider-state >/dev/null
          )

      - name: Smoke test knowledge DB initialization
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          (
            cd "${target}"
            .totonoe/bin/init_knowledge.sh
            test -f .totonoe/knowledge.db
            ver="$(sqlite3 .totonoe/knowledge.db 'SELECT MAX(version) FROM schema_version;')"
            test "${ver}" = "2"
            tables="$(sqlite3 .totonoe/knowledge.db ".tables")"
            echo "${tables}" | grep -q "review_rounds"
            echo "${tables}" | grep -q "review_findings"
            echo "${tables}" | grep -q "verdicts"
            echo "${tables}" | grep -q "lessons"
            echo "${tables}" | grep -q "schema_version"
            sqlite3 .totonoe/knowledge.db "SELECT resolved FROM review_findings LIMIT 0;"
            mode="$(sqlite3 .totonoe/knowledge.db 'PRAGMA journal_mode;')"
            test "${mode}" = "wal"
            .totonoe/bin/init_knowledge.sh
            .totonoe/bin/init_knowledge.sh --force
            ver2="$(sqlite3 .totonoe/knowledge.db 'SELECT MAX(version) FROM schema_version;')"
            test "${ver2}" = "2"
          )

      - name: Smoke test knowledge write layer
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          fake_bin="$(mktemp -d)"
          cat > "${fake_bin}/codex" <<'FAKESCRIPT'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*)
    cat <<JSON
{"summary":"CI test","overall_grade":"${FAKE_GRADE:-A}","critical_count":1,"findings":[{"file":"src/app.ts","severity":"critical","title":"テスト指摘","reason":"テスト理由","suggested_fix":"修正案"}]}
JSON
    ;;
  *judge*)
    cat <<JSON
{"recommendation":"fix","reason":"修正必要","must_fix":["テスト指摘"],"can_defer":[],"next_step":"修正する","engineer_type":"generic","spot_check_required":false}
JSON
    ;;
esac
FAKESCRIPT
          chmod +x "${fake_bin}/codex"
          export PATH="${fake_bin}:${PATH}"
          (
            cd "${target}"
            .totonoe/bin/init_job.sh --job-name write-test --goal-template feature_loop --with-knowledge
            test -f .totonoe/knowledge.db
            test "$(jq -r '.knowledge_enabled' .totonoe/runtime/write-test/state.json)" = "true"
            .totonoe/bin/init_job.sh --job-name no-kn --goal-template feature_loop
            test "$(jq -r '.knowledge_enabled' .totonoe/runtime/no-kn/state.json)" = "false"
            printf 'summary\n' > .totonoe/runtime/write-test/summary.md
            .totonoe/bin/record_claude_round.sh --job-name write-test --summary-file .totonoe/runtime/write-test/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp=".totonoe/runtime/write-test/rounds/001"
            mkdir -p "${rp}/snapshot" && echo "d" > "${rp}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds;')" = "1"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT overall_grade FROM review_rounds;')" = "A"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_findings;')" = "1"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT severity FROM review_findings;')" = "critical"
            .totonoe/bin/run_judge.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM verdicts;')" = "1"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT recommendation FROM verdicts;')" = "fix"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT engineer_type FROM verdicts;')" = "generic"
            .totonoe/bin/run_reviewer.sh --job-name write-test --force
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds;')" = "1"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_findings;')" = "1"
            .totonoe/bin/run_judge.sh --job-name write-test --force
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM verdicts;')" = "1"
            export FAKE_GRADE="C"
            jq '.status = "init" | .current_round = 0' .totonoe/runtime/write-test/state.json > /tmp/_st.json && mv /tmp/_st.json .totonoe/runtime/write-test/state.json
            printf 'summary\n' > .totonoe/runtime/write-test/summary.md
            .totonoe/bin/record_claude_round.sh --job-name write-test --summary-file .totonoe/runtime/write-test/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp2=".totonoe/runtime/write-test/rounds/002"
            mkdir -p "${rp2}/snapshot" && echo "d" > "${rp2}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds;')" = "1"
            export FAKE_GRADE="B"
            jq '.status = "init" | .current_round = 0' .totonoe/runtime/write-test/state.json > /tmp/_st.json && mv /tmp/_st.json .totonoe/runtime/write-test/state.json
            printf 'summary\n' > .totonoe/runtime/write-test/summary.md
            .totonoe/bin/record_claude_round.sh --job-name write-test --summary-file .totonoe/runtime/write-test/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp3=".totonoe/runtime/write-test/rounds/003"
            mkdir -p "${rp3}/snapshot" && echo "d" > "${rp3}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE overall_grade = "B";')" = "1"
            jq '.knowledge.quality_threshold_count = 1' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
            jq '.status = "init" | .current_round = 0' .totonoe/runtime/write-test/state.json > /tmp/_st.json && mv /tmp/_st.json .totonoe/runtime/write-test/state.json
            printf 'summary\n' > .totonoe/runtime/write-test/summary.md
            .totonoe/bin/record_claude_round.sh --job-name write-test --summary-file .totonoe/runtime/write-test/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp4=".totonoe/runtime/write-test/rounds/004"
            mkdir -p "${rp4}/snapshot" && echo "d" > "${rp4}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE overall_grade = "B";')" = "1"
            jq '.knowledge.quality_threshold_count = 20' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
            cat > "${fake_bin}/codex" <<'FAKESCRIPT2'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*) echo '{"summary":"完璧","overall_grade":"S","critical_count":0,"findings":[]}' ;;
  *judge*) echo '{"recommendation":"done","reason":"完了","must_fix":[],"can_defer":[],"next_step":"なし","engineer_type":"generic","spot_check_required":false}' ;;
esac
FAKESCRIPT2
            chmod +x "${fake_bin}/codex"
            jq '.status = "init" | .current_round = 0' .totonoe/runtime/write-test/state.json > /tmp/_st.json && mv /tmp/_st.json .totonoe/runtime/write-test/state.json
            printf 'summary\n' > .totonoe/runtime/write-test/summary.md
            .totonoe/bin/record_claude_round.sh --job-name write-test --summary-file .totonoe/runtime/write-test/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp5=".totonoe/runtime/write-test/rounds/005"
            mkdir -p "${rp5}/snapshot" && echo "d" > "${rp5}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE overall_grade = "S";')" -ge 1
            test "$(sqlite3 .totonoe/knowledge.db "SELECT COUNT(*) FROM review_findings WHERE review_round_id = (SELECT id FROM review_rounds WHERE overall_grade = 'S' ORDER BY id DESC LIMIT 1);")" = "0"
            printf 'summary\n' > .totonoe/runtime/no-kn/summary.md
            .totonoe/bin/record_claude_round.sh --job-name no-kn --summary-file .totonoe/runtime/no-kn/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp_nk=".totonoe/runtime/no-kn/rounds/001"
            mkdir -p "${rp_nk}/snapshot" && echo "d" > "${rp_nk}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name no-kn
            .totonoe/bin/run_judge.sh --job-name no-kn
            cat > "${fake_bin}/codex" <<'FAKESCRIPT3'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*) printf '{"summary":"t","overall_grade":"%s","critical_count":0,"findings":[]}\n' "${FAKE_GRADE:-A}" ;;
  *judge*) echo '{"recommendation":"done","reason":"ok","must_fix":[],"can_defer":[],"next_step":"n","engineer_type":"generic","spot_check_required":false}' ;;
esac
FAKESCRIPT3
            chmod +x "${fake_bin}/codex"
            export FAKE_GRADE="B"
            jq '.status = "init" | .current_round = 0' .totonoe/runtime/write-test/state.json > /tmp/_st.json && mv /tmp/_st.json .totonoe/runtime/write-test/state.json
            printf 'summary\n' > .totonoe/runtime/write-test/summary.md
            .totonoe/bin/record_claude_round.sh --job-name write-test --summary-file .totonoe/runtime/write-test/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            rp6=".totonoe/runtime/write-test/rounds/006"
            mkdir -p "${rp6}/snapshot" && echo "d" > "${rp6}/snapshot/setup.sh"
            .totonoe/bin/run_reviewer.sh --job-name write-test
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE overall_grade = "B" AND round = 6;')" = "1"
            jq '.knowledge.quality_threshold_count = 1' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
            .totonoe/bin/run_reviewer.sh --job-name write-test --force
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE overall_grade = "B" AND round = 6;')" = "0"
            jq '.knowledge.quality_threshold_count = 20' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
            export FAKE_GRADE="A"
            .totonoe/bin/run_reviewer.sh --job-name write-test --force
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE overall_grade = "A" AND round = 6;')" = "1"
            export FAKE_GRADE="C"
            .totonoe/bin/run_reviewer.sh --job-name write-test --force
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_rounds WHERE round = 6;')" = "0"
            jq '.knowledge.quality_threshold_count = "bad"' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
            export FAKE_GRADE="A"
            .totonoe/bin/run_reviewer.sh --job-name write-test --force 2>&1 | grep -q "不正です"
            jq '.knowledge.quality_threshold_count = 20' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
          )

      - name: Smoke test knowledge query and judge injection
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          fake_bin="$(mktemp -d)"
          cat > "${fake_bin}/codex" <<'FAKESCRIPT'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*) echo '{"summary":"test","overall_grade":"A","critical_count":1,"findings":[{"file":"a.ts","severity":"critical","title":"NULL","reason":"null check","suggested_fix":"fix"}]}' ;;
  *judge*) echo '{"recommendation":"fix","reason":"critical","must_fix":["NULL"],"can_defer":[],"next_step":"fix","engineer_type":"security","spot_check_required":false}' ;;
esac
FAKESCRIPT
          chmod +x "${fake_bin}/codex"
          export PATH="${fake_bin}:${PATH}"
          (
            cd "${target}"
            .totonoe/bin/init_job.sh --job-name qt --goal-template feature_loop --with-knowledge
            printf 'summary\n' > .totonoe/runtime/qt/summary.md
            .totonoe/bin/record_claude_round.sh --job-name qt --summary-file .totonoe/runtime/qt/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/qt/rounds/001/snapshot && echo "d" > .totonoe/runtime/qt/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name qt
            .totonoe/bin/run_judge.sh --job-name qt
            .totonoe/bin/query_knowledge.sh --type findings | grep -q "critical"
            .totonoe/bin/query_knowledge.sh --type verdicts | grep -q "fix"
            .totonoe/bin/query_knowledge.sh --type summary | grep -q "ジョブ数"
            rm .totonoe/knowledge.db
            set +e; .totonoe/bin/query_knowledge.sh --type summary 2>/dev/null; test "$?" = "2"; set -e
            .totonoe/bin/init_knowledge.sh >/dev/null 2>&1
            set +e; .totonoe/bin/query_knowledge.sh --type findings 2>/dev/null; test "$?" = "1"; set -e
            for i in 1 2 3 4 5; do
              sqlite3 .totonoe/knowledge.db "PRAGMA foreign_keys=ON; INSERT OR REPLACE INTO review_rounds (job_name, round, overall_grade, critical_count, summary) VALUES ('b', ${i}, 'A', 1, 't${i}');"
              sqlite3 .totonoe/knowledge.db "PRAGMA foreign_keys=ON; INSERT INTO review_findings (review_round_id, job_name, round, file, severity, title, reason) VALUES ((SELECT id FROM review_rounds WHERE job_name='b' AND round=${i}), 'b', ${i}, 'f.ts', 'high', 'i${i}', 'r${i}');"
            done
            test "$(.totonoe/bin/query_knowledge.sh --type findings | grep -c '^\-')" = "3"
            .totonoe/bin/query_knowledge.sh --type findings --limit 5 --max-chars 30 | grep -q "truncated"
            sqlite3 .totonoe/knowledge.db "UPDATE review_findings SET resolved = 1;"
            set +e; .totonoe/bin/query_knowledge.sh --type findings 2>/dev/null; test "$?" = "1"; set -e
            sqlite3 .totonoe/knowledge.db "UPDATE review_findings SET resolved = 0;"
            sqlite3 .totonoe/knowledge.db "PRAGMA foreign_keys=ON;
              INSERT OR REPLACE INTO review_rounds (job_name, round, overall_grade, critical_count, summary) VALUES ('pipe', 1, 'A', 1, 's');
              INSERT INTO review_findings (review_round_id, job_name, round, file, severity, title, reason)
                VALUES ((SELECT id FROM review_rounds WHERE job_name='pipe' AND round=1), 'pipe', 1, 'a|b.ts', 'critical', 'title|pipe', 'reason|pipe');
              INSERT OR REPLACE INTO verdicts (job_name, round, recommendation, engineer_type, reason) VALUES ('pipe', 1, 'fix', 'generic', 'reason|pipe');"
            .totonoe/bin/query_knowledge.sh --type findings --job-name pipe | grep -q "title|pipe"
            .totonoe/bin/query_knowledge.sh --type verdicts | grep -q "reason|pipe"
            if .totonoe/bin/query_knowledge.sh --type findings --limit bad 2>/dev/null; then exit 1; fi
            if .totonoe/bin/query_knowledge.sh --type findings --limit 0 2>/dev/null; then exit 1; fi
            for i in 1 2 3; do
              sqlite3 .totonoe/knowledge.db "INSERT OR REPLACE INTO verdicts (job_name, round, recommendation, engineer_type, reason) VALUES ('b', ${i}, 'fix', 'generic', 'test${i}');"
            done
            .totonoe/bin/init_job.sh --job-name qt2 --goal-template feature_loop --with-knowledge --force
            printf 'summary\n' > .totonoe/runtime/qt2/summary.md
            .totonoe/bin/record_claude_round.sh --job-name qt2 --summary-file .totonoe/runtime/qt2/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/qt2/rounds/001/snapshot && echo "d" > .totonoe/runtime/qt2/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name qt2
            .totonoe/bin/run_judge.sh --job-name qt2
            pf=".totonoe/runtime/qt2/rounds/001/judge.prompt.md"
            grep -q "参考情報" "${pf}"
            grep -q "独立に判断" "${pf}"
            grep -q "引きずられず" "${pf}"
            .totonoe/bin/init_job.sh --job-name nokn --goal-template feature_loop
            printf 'summary\n' > .totonoe/runtime/nokn/summary.md
            .totonoe/bin/record_claude_round.sh --job-name nokn --summary-file .totonoe/runtime/nokn/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/nokn/rounds/001/snapshot && echo "d" > .totonoe/runtime/nokn/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name nokn
            .totonoe/bin/run_judge.sh --job-name nokn
            ! grep -q "参考情報" .totonoe/runtime/nokn/rounds/001/judge.prompt.md
          )

      - name: Smoke test reviewer injection
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          fake_bin="$(mktemp -d)"
          cat > "${fake_bin}/codex" <<'FAKESCRIPT'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*) echo '{"summary":"test","overall_grade":"A","critical_count":1,"findings":[{"file":"a.ts","severity":"critical","title":"NULL","reason":"null check","suggested_fix":"fix"}]}' ;;
  *judge*) echo '{"recommendation":"fix","reason":"critical","must_fix":["NULL"],"can_defer":[],"next_step":"fix","engineer_type":"generic","spot_check_required":false}' ;;
esac
FAKESCRIPT
          chmod +x "${fake_bin}/codex"
          export PATH="${fake_bin}:${PATH}"
          (
            cd "${target}"

            # seed ジョブで findings を蓄積
            .totonoe/bin/init_job.sh --job-name seed --goal-template feature_loop --with-knowledge
            printf 'summary\n' > .totonoe/runtime/seed/summary.md
            .totonoe/bin/record_claude_round.sh --job-name seed --summary-file .totonoe/runtime/seed/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/seed/rounds/001/snapshot && echo "d" > .totonoe/runtime/seed/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name seed

            # 別ジョブで reviewer 実行 → prompt に注入されるか
            .totonoe/bin/init_job.sh --job-name rv --goal-template feature_loop --with-knowledge
            printf 'summary\n' > .totonoe/runtime/rv/summary.md
            .totonoe/bin/record_claude_round.sh --job-name rv --summary-file .totonoe/runtime/rv/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/rv/rounds/001/snapshot && echo "d" > .totonoe/runtime/rv/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name rv
            rpf=".totonoe/runtime/rv/rounds/001/reviewer_batch_001.prompt.md"
            grep -q "過去の未解決指摘" "${rpf}"
            grep -q "参考情報にすぎません" "${rpf}"
            grep -q "独立にレビュー" "${rpf}"
            grep -q "見逃さない" "${rpf}"

            # knowledge 無効ジョブでは注入なし
            .totonoe/bin/init_job.sh --job-name nokn --goal-template feature_loop
            printf 'summary\n' > .totonoe/runtime/nokn/summary.md
            .totonoe/bin/record_claude_round.sh --job-name nokn --summary-file .totonoe/runtime/nokn/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/nokn/rounds/001/snapshot && echo "d" > .totonoe/runtime/nokn/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name nokn
            ! grep -q "過去の未解決指摘" .totonoe/runtime/nokn/rounds/001/reviewer_batch_001.prompt.md

            # resolved=1 の findings は注入されない
            sqlite3 .totonoe/knowledge.db "UPDATE review_findings SET resolved = 1;"
            .totonoe/bin/init_job.sh --job-name rv2 --goal-template feature_loop --with-knowledge
            printf 'summary\n' > .totonoe/runtime/rv2/summary.md
            .totonoe/bin/record_claude_round.sh --job-name rv2 --summary-file .totonoe/runtime/rv2/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/rv2/rounds/001/snapshot && echo "d" > .totonoe/runtime/rv2/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name rv2
            ! grep -q "過去の未解決指摘" .totonoe/runtime/rv2/rounds/001/reviewer_batch_001.prompt.md
          )

      - name: Smoke test lessons and manager hook
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          fake_bin="$(mktemp -d)"
          cat > "${fake_bin}/codex" <<'FAKESCRIPT'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*) echo '{"summary":"clean","overall_grade":"S","critical_count":0,"findings":[]}' ;;
  *judge*) echo '{"recommendation":"done","reason":"完了","must_fix":[],"can_defer":[],"next_step":"none","engineer_type":"generic","spot_check_required":false}' ;;
esac
FAKESCRIPT
          chmod +x "${fake_bin}/codex"
          export PATH="${fake_bin}:${PATH}"
          (
            cd "${target}"
            .totonoe/bin/init_job.sh --job-name lt --goal-template feature_loop --with-knowledge
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT MAX(version) FROM schema_version;')" = "2"
            sqlite3 .totonoe/knowledge.db ".tables" | grep -q "lessons"
            sqlite3 .totonoe/knowledge.db "PRAGMA foreign_keys=ON;
              INSERT INTO review_rounds (job_name, round, overall_grade, critical_count, summary) VALUES ('lt', 1, 'A', 1, 'test');
              INSERT INTO review_findings (review_round_id, job_name, round, file, severity, title, reason) VALUES (1, 'lt', 1, 'a.ts', 'high', 'bug', 'reason');"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_findings WHERE resolved = 0 AND job_name = "lt";')" = "1"
            .totonoe/bin/learn.sh --job-name lt --lesson "テスト教訓"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_findings WHERE resolved = 1 AND job_name = "lt";')" = "1"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT lesson FROM lessons WHERE job_name = "lt";')" = "テスト教訓"
            .totonoe/bin/learn.sh --job-name lt --lesson "更新"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM lessons WHERE job_name = "lt";')" = "1"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT lesson FROM lessons WHERE job_name = "lt";')" = "更新"
            sqlite3 .totonoe/knowledge.db "UPDATE review_findings SET resolved = 0 WHERE job_name = 'lt';"
            .totonoe/bin/learn.sh --job-name lt
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM review_findings WHERE resolved = 1 AND job_name = "lt";')" = "1"
            .totonoe/bin/init_job.sh --job-name nokn --goal-template feature_loop
            .totonoe/bin/learn.sh --job-name nokn --lesson "nope"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM lessons WHERE job_name = "nokn";')" = "0"
            .totonoe/bin/init_job.sh --job-name lt2 --goal-template feature_loop --with-knowledge --force
            printf 'summary\n' > .totonoe/runtime/lt2/summary.md
            .totonoe/bin/record_claude_round.sh --job-name lt2 --summary-file .totonoe/runtime/lt2/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/lt2/rounds/001/snapshot && echo "d" > .totonoe/runtime/lt2/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name lt2
            .totonoe/bin/run_judge.sh --job-name lt2
            .totonoe/bin/apply_manager_decision.sh --job-name lt2 --record-spot-check
            .totonoe/bin/apply_manager_decision.sh --job-name lt2 --decision done --lesson "E2E教訓"
            test "$(jq -r '.status' .totonoe/runtime/lt2/state.json)" = "done"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT lesson FROM lessons WHERE job_name = "lt2";')" = "E2E教訓"
            cat > "${fake_bin}/codex" <<'FAKESCRIPT2'
#!/usr/bin/env bash
schema_file=""
while [ "$#" -gt 0 ]; do
  case "$1" in --output-schema) schema_file="$2"; shift 2 ;; *) shift ;; esac
done
case "${schema_file}" in
  *reviewer*) echo '{"summary":"bad","overall_grade":"C","critical_count":2,"findings":[{"file":"a.ts","severity":"critical","title":"b1","reason":"r","suggested_fix":"f"},{"file":"b.ts","severity":"critical","title":"b2","reason":"r","suggested_fix":"f"}]}' ;;
  *judge*) echo '{"recommendation":"fix","reason":"要修正","must_fix":["b1"],"can_defer":[],"next_step":"修正","engineer_type":"generic","spot_check_required":false}' ;;
esac
FAKESCRIPT2
            chmod +x "${fake_bin}/codex"
            .totonoe/bin/init_job.sh --job-name lt3 --goal-template feature_loop --with-knowledge --force
            printf 'summary\n' > .totonoe/runtime/lt3/summary.md
            .totonoe/bin/record_claude_round.sh --job-name lt3 --summary-file .totonoe/runtime/lt3/summary.md --changed-file setup.sh --quality-analyze passed --quality-test skipped
            mkdir -p .totonoe/runtime/lt3/rounds/001/snapshot && echo "d" > .totonoe/runtime/lt3/rounds/001/snapshot/setup.sh
            .totonoe/bin/run_reviewer.sh --job-name lt3
            .totonoe/bin/run_judge.sh --job-name lt3
            .totonoe/bin/apply_manager_decision.sh --job-name lt3 --record-spot-check
            .totonoe/bin/apply_manager_decision.sh --job-name lt3 --decision done --lesson "nope"
            test "$(jq -r '.status' .totonoe/runtime/lt3/state.json)" = "human"
            test "$(sqlite3 .totonoe/knowledge.db 'SELECT COUNT(*) FROM lessons WHERE job_name = "lt3";')" = "0"
            if .totonoe/bin/apply_manager_decision.sh --job-name lt3 --decision fix --lesson "bad" --force 2>/dev/null; then exit 1; fi
            if .totonoe/bin/apply_manager_decision.sh --job-name lt3 --decision continue --lesson "bad" --force 2>/dev/null; then exit 1; fi
            .totonoe/bin/query_knowledge.sh --type lessons | grep -q "E2E教訓"
            test "$(.totonoe/bin/query_knowledge.sh --type lessons --limit 1 | grep -c '^\-')" = "1"
            .totonoe/bin/query_knowledge.sh --type lessons --max-chars 10 | grep -q "truncated"
          )

      - name: Smoke test shadow status generation
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          (
            cd "${target}"
            jq '.reviewer.mode = "shadow"' .totonoe/config.json > /tmp/_cfg.json && mv /tmp/_cfg.json .totonoe/config.json
            .totonoe/bin/init_job.sh --job-name shadow-smoke --goal-text "shadow ci test"
            printf 'summary\n' > .totonoe/runtime/shadow-smoke/summary.md
            .totonoe/bin/record_claude_round.sh \
              --job-name shadow-smoke \
              --summary-file .totonoe/runtime/shadow-smoke/summary.md \
              --changed-file CLAUDE.totonoe.template.md \
              --quality-analyze passed \
              --quality-test skipped
            round_path=".totonoe/runtime/shadow-smoke/rounds/001"
            source .totonoe/bin/common.sh
            write_shadow_status() {
              local rp="$1"; shift; local entries=("$@")
              if [ "${#entries[@]}" -eq 0 ]; then
                jq -n '{mode: "shadow", batches: []}' > "${rp}/reviewer_shadow_status.json"
                return
              fi
              printf '%s\n' "${entries[@]}" | jq -s '{mode: "shadow", batches: .}' > "${rp}/reviewer_shadow_status.json"
            }
            entries=()
            entries+=("$(jq -nc '{batch:"001",status:"success",provider:"gemini"}')")
            entries+=("$(jq -nc '{batch:"002",status:"skipped",reason:"primary_used_gemini_fallback"}')")
            entries+=("$(jq -nc '{batch:"003",status:"failed",reason:"shadow_execution_failed"}')")
            write_shadow_status "${round_path}" "${entries[@]}"
            sf="${round_path}/reviewer_shadow_status.json"
            test -f "${sf}"
            test "$(jq -r '.mode' "${sf}")" = "shadow"
            test "$(jq '.batches | length' "${sf}")" -eq 3
            test "$(jq -r '.batches[0].status' "${sf}")" = "success"
            test "$(jq -r '.batches[1].status' "${sf}")" = "skipped"
            test "$(jq -r '.batches[1].reason' "${sf}")" = "primary_used_gemini_fallback"
            test "$(jq -r '.batches[2].status' "${sf}")" = "failed"
            test "$(jq -r '.batches[2].reason' "${sf}")" = "shadow_execution_failed"
          )

      - name: Smoke test stale lock recovery (mkdir fallback)
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          (
            cd "${target}"
            export TOTONOE_FORCE_MKDIR_LOCK=1
            .totonoe/bin/init_job.sh --job-name locktest --goal-text "lock ci test"
            lock_dir=".totonoe/runtime/locktest/.job.lock.d"
            mkdir -p "${lock_dir}"
            jq -nc '{pid: 99999999, hostname: "ci", created_at: "2025-01-01T00:00:00Z"}' > "${lock_dir}/owner.json"
            test -d "${lock_dir}"
            printf 'summary\n' > .totonoe/runtime/locktest/summary.md
            .totonoe/bin/record_claude_round.sh \
              --job-name locktest \
              --summary-file .totonoe/runtime/locktest/summary.md \
              --changed-file CLAUDE.totonoe.template.md \
              --quality-analyze passed \
              --quality-test skipped
            status="$(.totonoe/bin/status.sh --job-name locktest --json | jq -r '.status')"
            test "${status}" = "reviewing"
            test ! -d "${lock_dir}"
            mkdir -p "${lock_dir}"
            jq -nc --argjson pid "$$" '{pid: $pid, hostname: "ci", created_at: "2025-01-01T00:00:00Z"}' > "${lock_dir}/owner.json"
            if timeout 3 bash -c "
              cd '${target}'
              export TOTONOE_FORCE_MKDIR_LOCK=1
              source .totonoe/bin/common.sh
              acquire_job_lock locktest
            " 2>/dev/null; then
              echo 'FAIL: live PID lock was stolen' >&2; exit 1
            fi
            rm -rf "${lock_dir}"
            mkdir -p "${lock_dir}"
            if timeout 3 bash -c "
              cd '${target}'
              export TOTONOE_FORCE_MKDIR_LOCK=1
              source .totonoe/bin/common.sh
              acquire_job_lock locktest
            " 2>/dev/null; then
              echo 'FAIL: lock without metadata was reclaimed within grace period' >&2; exit 1
            fi
            rm -rf "${lock_dir}"
          )

      - name: Smoke test v2 migration
        run: |
          # v2 配置をセットアップ（旧パス .claude/totonoe/ にコピー）
          target="$(mktemp -d)"
          mkdir -p "${target}/.claude/totonoe"
          cp -R .totonoe/bin "${target}/.claude/totonoe/bin"
          cp -R .totonoe/migrations "${target}/.claude/totonoe/migrations"
          cp -R .totonoe/schemas "${target}/.claude/totonoe/schemas"
          cp -R .totonoe/goals "${target}/.claude/totonoe/goals"
          echo "v2.0.0" > "${target}/.claude/totonoe/VERSION"
          cp .totonoe/RUNBOOK.md "${target}/.claude/totonoe/RUNBOOK.md"
          cp .totonoe/README.md "${target}/.claude/totonoe/README.md"
          cp .totonoe/SUPERVISOR.md "${target}/.claude/totonoe/SUPERVISOR.md"
          cp .totonoe/config.json "${target}/.claude/totonoe/config.json"
          chmod +x "${target}"/.claude/totonoe/bin/*.sh
          mkdir -p "${target}/.claude/totonoe/runtime/test-job"
          echo '{"status":"init"}' > "${target}/.claude/totonoe/runtime/test-job/state.json"
          mkdir -p "${target}/.claude"
          echo '{"permissions":{"allow":["Bash(.claude/totonoe/bin/init_job.sh:*)"]}}' > "${target}/.claude/settings.json"
          echo '.claude/totonoe/runtime/' > "${target}/.gitignore"
          sqlite3 "${target}/.claude/totonoe/knowledge.db" "CREATE TABLE test(id INTEGER);"
          ./setup.sh --target "${target}" --migrate-v2
          test -d "${target}/.totonoe"
          test -d "${target}/.totonoe/bin"
          test -x "${target}/.totonoe/bin/common.sh"
          test ! -d "${target}/.claude/totonoe"
          test -d "${target}/.totonoe/runtime/test-job"
          test -f "${target}/.totonoe/runtime/test-job/state.json"
          test -f "${target}/.totonoe/knowledge.db"
          test -f "${target}/.totonoe/config.json"
          grep -q '.totonoe/bin/' "${target}/.claude/settings.json"
          ! grep -q '.claude/totonoe/bin/' "${target}/.claude/settings.json"
          grep -q '.totonoe/runtime/' "${target}/.gitignore"
          ! grep -q '.claude/totonoe/runtime/' "${target}/.gitignore"
          (
            cd "${target}"
            .totonoe/bin/init_job.sh --job-name migrate-smoke --goal-text "migration test" --force
            .totonoe/bin/status.sh --job-name migrate-smoke --json >/dev/null
          )

      - name: Smoke test v2 migration (no old gitignore lines + template refresh)
        run: |
          target="$(mktemp -d)"
          mkdir -p "${target}/.claude/totonoe"
          cp -R .totonoe/bin "${target}/.claude/totonoe/bin"
          cp -R .totonoe/migrations "${target}/.claude/totonoe/migrations"
          cp -R .totonoe/schemas "${target}/.claude/totonoe/schemas"
          cp -R .totonoe/goals "${target}/.claude/totonoe/goals"
          echo "v2.0.0" > "${target}/.claude/totonoe/VERSION"
          cp .totonoe/RUNBOOK.md "${target}/.claude/totonoe/RUNBOOK.md"
          cp .totonoe/README.md "${target}/.claude/totonoe/README.md"
          cp .totonoe/SUPERVISOR.md "${target}/.claude/totonoe/SUPERVISOR.md"
          cp .totonoe/config.json "${target}/.claude/totonoe/config.json"
          chmod +x "${target}"/.claude/totonoe/bin/*.sh
          mkdir -p "${target}/.claude"
          echo '{"permissions":{"allow":["Bash(.claude/totonoe/bin/init_job.sh:*)"]}}' > "${target}/.claude/settings.json"
          # .gitignore に旧パス行がない状態
          echo '.env' > "${target}/.gitignore"
          # 旧パスのテンプレートファイル
          echo 'ランタイムパスは .claude/totonoe/ です。' > "${target}/CLAUDE.totonoe.template.md"
          echo 'ランタイムパスは .claude/totonoe/ です。' > "${target}/AGENTS.totonoe.template.md"
          ./setup.sh --target "${target}" --migrate-v2
          # .gitignore に必要な行が入っているか
          grep -q '.totonoe/runtime/' "${target}/.gitignore"
          grep -q '.totonoe/knowledge.db$' "${target}/.gitignore"
          grep -q '.totonoe/knowledge.db-wal' "${target}/.gitignore"
          grep -q '.totonoe/knowledge.db-shm' "${target}/.gitignore"
          # テンプレートが v3 内容に更新されているか
          ! grep -q '.claude/totonoe/' "${target}/CLAUDE.totonoe.template.md"
          grep -q '.totonoe/' "${target}/CLAUDE.totonoe.template.md"
          ! grep -q '.claude/totonoe/' "${target}/AGENTS.totonoe.template.md"
          grep -q '.totonoe/' "${target}/AGENTS.totonoe.template.md"

      - name: Smoke test repo root with .git file
        run: |
          target="$(mktemp -d)"
          ./setup.sh --target "${target}" --force
          echo "gitdir: /tmp/fake-git-dir" > "${target}/.git"
          (
            cd "${target}"
            .totonoe/bin/init_job.sh --job-name gitfile-test --goal-text "git file test" --force
            .totonoe/bin/status.sh --job-name gitfile-test --json >/dev/null
          )
